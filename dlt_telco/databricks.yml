# This is a Databricks asset bundle definition for dlt_telco.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dlt_telco
  uuid: 0c7f173c-e9d5-4717-818c-e5fc3ac28e0d

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-ecbdc66c-a5d4.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-ecbdc66c-a5d4.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/pat@clouddata.engineer to make sure we only have a single copy.
      root_path: /Workspace/Users/pat@clouddata.engineer/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: pat@clouddata.engineer
        level: CAN_MANAGE
