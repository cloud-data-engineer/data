# Streaming Bronze Layer DLT Pipeline Configuration
resources:
  pipelines:
    dlt_telco:
      name: dlt_telco
      catalog: ${bundle.target}_bronze
      schema: cdr
      serverless: true
      
      libraries:
        - notebook:
            path: ../src/bronze.py
        - notebook:
            path: ../src/silver.py
      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
        env_scope: ${bundle.target}
        # Add any pipeline-specific configurations here
        # Example: source.path: "/path/to/your/json/files"
      
      development: ${var.development}
      
      # Notifications (optional)
      # notifications:
      #   - email_recipients:
      #       - "your-email@company.com"
      #     on_failure: true
      #     on_success: false
